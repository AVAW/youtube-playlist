{% if conversationPlaylistVideo %}
    {% if conversationPlaylistVideo.currentVideo %}
        {% for author in conversationPlaylistVideo.currentVideo.authors %}
            @{{- author.displayedName -}} 
        {% else %}
            Empty list of authors.
        {% endfor %}
    {% else %}
        {{ 'playlist.error.nothingIsPlayed'|trans }}
    {% endif %}
{% else %}
    {{ 'playlist.error.nothingIsPlayed'|trans }}
{% endif %}
